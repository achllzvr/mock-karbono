<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:background="@drawable/bg_card_hard_shadow"
    android:paddingBottom="16dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/imgCommunity"
        android:layout_width="0dp"
        android:layout_height="140dp"
        android:scaleType="centerCrop"
        tools:src="@drawable/img_nulp"
        android:background="#F0F0F0"
        app:shapeAppearanceOverlay="@style/RoundedTopImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvCommunityName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Community Name"
        android:textSize="14sp"
        android:textColor="@color/text_dark"
        android:textStyle="bold"
        android:layout_marginTop="12dp"
        android:layout_marginHorizontal="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/imgCommunity" />

    <TextView
        android:id="@+id/tvCommunityMemberCount"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="0 members"
        android:textSize="12sp"
        android:textColor="@color/text_secondary"
        android:layout_marginTop="4dp"
        android:layout_marginHorizontal="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvCommunityName" />

    <TextView
        android:id="@+id/tvCommunityTag"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:text="Eco-Positive"
        android:textSize="12sp"
        android:textColor="@color/cream_background"
        android:background="@drawable/bg_button_green"
        android:layout_marginTop="12dp"
        android:layout_marginHorizontal="12dp"
        android:padding="6dp"
        android:gravity="center"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvCommunityMemberCount" />


</androidx.constraintlayout.widget.ConstraintLayout>
